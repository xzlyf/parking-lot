<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xz.parking.dao.EmployeeDao">
    <insert id="save" parameterType="com.xz.parking.entity.po.AdminPo">
        insert into t_admin(employee_no,passwd,`name`,is_enable) value (#{employeeNo},#{passwd},#{name},#{isEnable})
    </insert>
    <update id="update" parameterType="com.xz.parking.entity.po.AdminPo">
        update t_admin
        <set>
            <if test="name!=null">name = #{name}</if>
            <if test="passwd!=null">passwd = #{passwd}</if>
            <if test="isEnable!=null">is_enable = #{isEnable}</if>
        </set>
        where id = #{id}
    </update>
    <delete id="deleteById">
        delete from t_admin where id = #{id}
    </delete>

    <resultMap id="adminMap" type="com.xz.parking.entity.po.AdminPo">
        <id column="id" property="id"/>
        <result column="employee_no" property="employeeNo"/>
        <result column="passwd" property="passwd"/>
        <result column="name" property="name"/>
        <result column="is_enable" property="isEnable"/>
        <collection property="roles" ofType="com.xz.parking.entity.po.RolePo">
            <result column="r_id" property="id"/>
            <result column="r_desc" property="description"/>
            <result column="r_name" property="name"/>
            <result column="r_scope" property="scope"/>
        </collection>
    </resultMap>

    <!--通过权限中间表查询对应权限-->
    <select id="findAll" resultMap="adminMap">
        select t.*,r.id as r_id ,r.description as r_desc ,r.name as r_name ,r.`scope` as r_scope
        from t_admin t
        left join  admin_role tr on t.id = tr.admin_id
        left join t_role r on tr.role_id =r.id
    </select>
    <select id="findRole" resultType="com.xz.parking.entity.po.RolePo">
        select * from t_role where id = #{id}
    </select>

    <select id="queryAdminByName" parameterType="String" resultType="com.xz.parking.entity.vo.AdminVo">
        select * from t_admin where name = #{name}
    </select>
    <select id="queryAdminByEmployeeNo" parameterType="String" resultType="com.xz.parking.entity.vo.AdminVo">
        select * from t_admin where employee_no = #{employeeNo}
    </select>
</mapper>